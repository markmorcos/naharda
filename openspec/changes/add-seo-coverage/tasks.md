# Tasks — add-seo-coverage

## Slice 1 — Sitemap + robots
- [ ] `@astrojs/sitemap` (per-lang, daily lastmod); `robots.txt` (allow + sitemap link).

## Slice 2 — Tier-1 intent pages (generated)
- [ ] Content collection of intent descriptors (currencies + karats); one EN + one ar-EG template.
- [ ] Generate `/eur|sar|aed|kwd|gbp-to-egp`, `/black-market-dollar`, `/gold-18k`, `/gold-24k` (+ `/ar/*`).
- [ ] Each: answer-first H1, live number, explainer, 30-day sparkline, API CTA.

## Slice 3 — Structured data + linking
- [ ] FAQPage + Dataset + BreadcrumbList JSON-LD per page.
- [ ] Internal link graph (home ⇄ intent pages ⇄ related); reciprocal hreflang.
- [ ] Lighthouse SEO ≥95 check.

## Slice 4 — OG images (stretch)
- [ ] Dynamic OG image (satori) with the current number per intent page.
