---
// Arabic is the default audience, so the bare landing (/) serves the Arabic
// homepage. We rewrite to /ar rather than redirect: the URL stays "/", and the
// rendered page keeps its own canonical (/ar), so /ar remains the single
// canonical Arabic home (no duplicate-content split). English lives at /en.
return Astro.rewrite("/ar");
---
